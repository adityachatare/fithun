<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/splash_background"
    android:fillViewport="true"
    tools:context=".ui.activities.loginFlow.SplashActivity">

        <androidx.constraintlayout.widget.Guideline
            android:id="@+id/guideline_horizontal_center"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            app:layout_constraintGuide_percent="0" />

        <ImageView
            android:id="@+id/mainLogo"
            android:layout_width="@dimen/_250sdp"
            android:layout_height="@dimen/_250sdp"
            android:src="@drawable/app_logo"
            app:layout_constraintTop_toTopOf="@+id/guideline_horizontal_center"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toTopOf="@+id/basChlteRhoIcon"
            app:layout_constraintTop_toBottomOf="@+id/guideline_horizontal_center"
            app:layout_constraintVertical_chainStyle="packed"
            android:contentDescription="@string/logo" />

        <!-- Bas Chlte Rho Icon -->
        <ImageView
            android:id="@+id/basChlteRhoIcon"
            android:layout_width="@dimen/_200sdp"
            android:layout_height="@dimen/_180sdp"
            android:layout_marginTop="@dimen/_12sdp"
            android:src="@drawable/bas_chalte_raho_icon"
            app:layout_constraintTop_toBottomOf="@+id/mainLogo"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:contentDescription="@string/logo"
            />


</androidx.constraintlayout.widget.ConstraintLayout>
